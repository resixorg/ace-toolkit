import { formatScoreField } from '../../../helpers/format-score-field';
import { module, test } from 'qunit';

module('Unit | Helper | format score field');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatScoreField(42);
  assert.ok(result);
});
